import styled from 'styled-components';

export const Container = styled.div`
  position: absolute;
  top: 0;
  right: 0;

  display: flex;
  padding: 30px;
  overflow: hidden;

  flex-direction: column;
`;
